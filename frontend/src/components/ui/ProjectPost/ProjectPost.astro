---
import { IconChevronLeft } from '@tabler/icons-react';
import PortableText from "@/components/PortableText.astro";
import Header from "./Header.astro";

const { title, body, imageUrl } = Astro.props;
---

<style lang="scss">
  article {
    margin: 60px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: white;

    .wrapper {
      margin: 0 auto;
      max-width: 1000px;
      width: 100%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 2rem;

      .breadcrumb {
        color: #bfbfbf;
        text-decoration: none;
        font-size: 1rem;
        font-weight: 500;
        margin-bottom: 1rem;
        display: inline-block;
        transition: all 0.2s ease-in-out;

        display: flex;
        align-items: center;
        gap: 0.25rem;

        &:hover {
          color: #f5f5f5;
        }
      }

      main {
        width: 100%;
        margin: 0 auto;
        font-size: 1.2rem;
        color: #e6e6e6;
        line-height: 1.6;
      }
    }
  }
</style>

<article>
  <div class="wrapper">
    <a class="breadcrumb" href="/">
      <IconChevronLeft style={{ width: '1.2rem', height: '1.2rem' }} />
      <span>go back</span>
    </a>

    <Header title={title} imageUrl={imageUrl} />
  
    <main>
      <PortableText portableText={body} />
    </main>
  </div>
</article>
